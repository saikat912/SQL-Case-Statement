# SQL-Case-Statement
The project focuses on using the SQL CASE statement to perform conditional logic, data transformation, categorization, aggregation, and reporting using the Employees sample database in PostgreSQL. I explored how CASE statements can be combined with SQL joins and aggregate functions to generate meaningful business insights from relational data.
